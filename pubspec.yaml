name: replicore
description: A pluggable local-first synchronization engine for Flutter.
version: 0.1.0
homepage: https://github.com/leonhardWullich/replicore

environment:
  sdk: ^3.10.8
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.4.2
  supabase_flutter: ^2.12.0 
  connectivity_plus: ^7.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter: